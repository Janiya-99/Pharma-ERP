import React from "react"; import { ERPListPage, StatusBadge } from "components/erp/ERPListPage"; import { batchRecalls } from "variables/mockData";
export default function BatchRecallPage() { return <ERPListPage title="Batch Recall" subtitle="Manage product batch recall events" searchKey="product" data={batchRecalls} onAdd={() => {}} addLabel="New Recall" columns={[ { key: "product", label: "Product" }, { key: "batchNo", label: "Batch No." }, { key: "reason", label: "Reason" }, { key: "raisedBy", label: "Raised By" }, { key: "date", label: "Date" }, { key: "affectedCustomers", label: "Affected Customers" }, { key: "status", label: "Status", render: (r: unknown) => <StatusBadge status={r.status} /> } ]} />; }
